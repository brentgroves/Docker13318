version: '3'

services:
   web:
     image: nginx
     hostname: nginx
     depends_on:
       - redis
     ports:
       - "8080:80"
   redis:
     image: redis
     hostname: redis
   apache:
     image: httpd
     hostname: apache
     ports: 
       - 80
